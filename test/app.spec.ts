import request from 'supertest';

import app from '../src/app';

describe('Status', () => {
  it('GET  -> /status', (done) => {
    done();
  });

  it('HEAD -> /status', (done) => {
    done();
  });
});
